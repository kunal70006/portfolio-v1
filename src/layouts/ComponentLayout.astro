---
export interface Props {
  id: string;
}

const { id } = Astro.props;
---

<article
  class="tracking-wider flex flex-col lg:flex-row min-h-screen lg:items-center justify-center lg:justify-normal text-justify"
  id={id}
>
  <slot />
</article>
